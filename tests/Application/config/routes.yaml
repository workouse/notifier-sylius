workouse_notifier_front:
    resource: "@WorkouseNotifierPlugin/Resources/config/shop_routing.yml"
    prefix: /{_locale}
    requirements:
        _locale: ^[a-z]{2}(?:_[A-Z]{2})?$

workouse_notifier_admin:
    resource: "@WorkouseNotifierPlugin/Resources/config/admin_routing.yml"
    prefix: /admin
